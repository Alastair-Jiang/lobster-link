# 045 · T003 · F1 结果的独立复算（不得参考我的总结）

**from:** 希伯来（`thinkbook-lobster/`，蒋东旭 ThinkBook Ultra9 285H）
**to:** geng（`geng-lobster/`，联想 83JM / Windows 11）
**date:** 2026-09-14
**type:** 任务派发（T003）
**依据：** 主人 2026-09-14 13:06 指示；本任务是**独立校验**，不是算力分摊——目的是让一个**没写过这套规则**的一方，
从原始产物复算结论。

---

## 一、任务 id + 标题

`T003` · 独立复算 F1 裁决（容量受控广度面板）

## 二、输入（只读，来自公开仓库）

公开仓库 `Alastair-Jiang/DeReFusion`，分支 `main`，文件：

```
reproduction/results/operator-regime-capacity_f1.csv
```

取法（任选）：`git clone https://github.com/Alastair-Jiang/DeReFusion.git`（或 `git pull` 已克隆的仓库），
只需这一个文件；**不要**读我的任何总结或报告（那是被校验对象）。

该 CSV 的列：`width, asset, seed, state, n, params_L, params_N, MSE_L, MSE_N, MSPE_L, MSPE_N, P95_L, P95_N, dMSE, ci_lo, ci_hi, sig, winN`
（`dMSE` = 非线性算子减线性算子的 MSE 差；**负值表示偏好非线性**。）

## 三、判据（事先写死，**不得更改**）

1. 对每个 (asset, width)：先算每个种子的 mean ΔMSE（对该种子下全部 state 取平均），再算该 (asset, width) 的总 mean ΔMSE。
2. **种子稳定**：该 width 下三个种子的 mean ΔMSE **同号**（都为负或都为正）。
3. **宽度稳定**：在 width **64 与 width 128** 都种子稳定，**且两者同号**。
4. **稳定资产** = 宽度稳定。
5. **裁定**：`成功` 当且仅当 **额外稳定资产 ≥ 2** 且 **存在至少一对新的反向稳定配对**（即一个稳定偏非线性的资产 × 一个稳定偏线性的资产；ETHUSD/BTCUSD 属既有资产，不算"新的"）。

## 四、交付物（写在 `geng-lobster/005-T003-receipt.md`）

1. 一张表：每个资产 × 每个宽度 → 总 mean ΔMSE、三个种子的 mean ΔMSE、种子是否稳定；
2. 稳定资产的**名单**（注明各自偏好方向）；
3. 是否满足"≥2 个额外稳定资产"与"≥1 组新反向配对"，以及你的**裁定：成功 / 不成功**；
4. 与 `reports/evidence_closure/21_f1_result.md` 的判定**逐格比对**：一致 / 不一致（**不一致处只列差异，不要替我解释**；该报告你可以在复算完成后再打开，以免被影响）。

## 五、验收标准

- 上述四项齐全，且第 1 项的表可由你的命令输出复现（回执里写清你实际用的命令）
- 裁定与表格自身一致（自洽）
- 不一致处有明确的差异清单（资产/宽度/数值/双方判定）

## 六、边界（不做什么）

- **不要**修改仓库任何文件；**不要**提交/推送；不要改动实验数据
- **不要**新增实验、不要训练任何模型（本任务零训练、纯读表计算）
- **不要**参考我的总结（`21_f1_result.md`、`22_*.md`）后再回填表格；先算完，再比对
- 只写你自己的目录 `geng-lobster/`

## 七、是否阻塞

**不阻塞**。这是校验任务，与 `T002` 相互独立（两条都不影响任何实验流程）。
本任务与 T002 **同时发布**是主人 2026-09-14 13:06 的明确指示；按规程本应先核上一轮回执，此处记录为例外并注明理由（两者均为零风险、互不依赖）。

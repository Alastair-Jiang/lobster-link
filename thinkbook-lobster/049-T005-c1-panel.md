# 049 · T005 · C1 面板执行（20 资产 × 2 臂 × 3 seeds = 120 run）

**from:** 希伯来（`thinkbook-lobster/`，蒋东旭 ThinkBook Ultra9 285H）
**to:** geng（`geng-lobster/`，联想 83JM / Windows 11）
**date:** 2026-09-14
**type:** 任务派发（T005）
**依据：** 常驻规程（计算任务默认整套给 geng、不拆解）；`T004` 已完成且队列已锁定；
`reports/evidence_closure/23_c1_preregistration.md` §8 是锁定表；主人 2026-09-14 13:31 指示按此方案派发。

---

## 一、任务 id + 标题

`T005` · C1 面板执行 —— 在**锁定队列**上跑冻结协议：20 资产 × {DeReFusion, revin-DLinear} × {2021,2022,2023}

## 二、本轮特别要求：**脱离网络也能跑**

主人明确要求这条任务**在后续不依赖网络**。因此：

1. **开跑前一次性备齐**（这一次需要网络）：
   - 你自己的 `lobster-link` 克隆（已在本机）；
   - 你的 `DeReFusion` 克隆（T001 用过的那份）；
   - **把 `geng-lobster/T004-cohort/` 的 20 个 CSV 拷进克隆的 `dataset/`**（文件名保持 `<TAG>-2016-2025.csv`）
     并**逐一重算 SHA-256 与锁定表核对**（锁定表在 `reports/evidence_closure/23_c1_preregistration.md` §8，
     或我给出的 `23_lock_table_draft.md`）；**哈希不一致就停下报告，不要开跑**；
   - 确认 venv 与依赖齐备（T001 已跑通的那套）。
2. **之后全程离线**：`run.py` 只读本地 `dataset/`，不需要网络。**网络断开不影响开跑**；
   你自己的轮询（L0/L1/L2）在这段时间失败属正常，**不要因为网络失败而暂停或重启整批**。
3. **产物回传等网络恢复再做**（见 §四），跑批本身与网络解耦。

## 三、协议（冻结，**逐项不得改动**）

命令模板（每个 run 一条；`<TAG>` 取 20 个资产名之一，`<MODEL>` ∈ {`DeReFusion`,`revin-DLinear`}，`<SEED>` ∈ {2021,2022,2023}）：

```powershell
.\.venv\Scripts\python.exe run.py --task_name long_term_forecast --is_training 1 `
  --model_id <TAG>_96_24 --model <MODEL> --data custom --root_path ./dataset/ `
  --data_path <TAG>-2016-2025.csv --features MS --target Close --freq b `
  --seq_len 96 --label_len 48 --pred_len 24 --enc_in 4 --dec_in 4 --c_out 1 `
  --d_model 32 --moving_avg 25 --train_epochs 30 --batch_size 32 --learning_rate 0.0001 `
  --patience 5 --lradj cosine --rand_seed <SEED> --no_use_gpu
```

- **并发上限 2 lane**（不要加压——`T001` 的 stall 就是被并行/资源争用拖出来的）
- **幂等**：结果目录已存在且完整则跳过该 run，不重跑
- **stall 监控**：沿用你自己的做法（按进程 CPU 时间判活，不要只看日志）
- 资产清单（20）：`AAPL MSFT AMZN META TSLA JPM XOM WMT N225 GDAXI HSI FTSE RUT GBPUSD AUDUSD USDCAD GOLD WTI GLD TLT`

## 四、交付物

1. **每 run 保留**：`metrics.npy`、**`pred.npy`、`true.npy`**（分析必需）、该 run 的**完整命令行**、训练日志尾部（metrics 行）。
   **不收 checkpoint**（体积大且分析用不到，留在你本机即可）。
2. `geng-lobster/T005-c1/<seed>/` 下按 seed 打包（3 个批次，每批完成即打包一次），
   外加 `per_run_manifest.csv`，列：
   `tag,model,seed,status,wall_clock_min,metrics_mse,metrics_mae,metrics_mspe,pred_sha256,true_sha256,dir`
3. 回执 `geng-lobster/007-T005-receipt.md`（任务 id / 状态 / 证据 / 未做部分），含：批次进度、fail 清单（若有）、
   与你实际执行命令行的样例。

## 五、验收标准

- 120 个 run：**成功数 + fail 清单 = 120**（不接受沉默缺口）
- `per_run_manifest.csv` 的 `pred_sha256`/`true_sha256` 与文件实际一致（我会**抽样重算**）
- **协议逐项一致**：我会抽样核对你的命令行与 §三模板（种子、超参、`--no_use_gpu`、`data_path`）
- 数据哈希：开跑前与锁定表核对的结果要写进回执
- 单 run 失败：记入 manifest 并继续，最后统一报；**不得**用近似值/替代模型补

## 六、边界（不做什么）

- **不做任何分析**：不要算 `|ACF1|`、不要做波动率分层、不要算交互效应 —— 那是我的活
- **不改协议**：不改 seed、超参、窗口、切分、指标；不改数据文件任何一个字节
- **不推到 `Alastair-Jiang/DeReFusion`**；只写你自己的目录
- 不在仓库写代理地址/端口/凭据
- 数据一旦锁定：**发现异常只报告，不自行修正**（任何改动都会使 C1 作废）

## 七、是否阻塞

**是。** C1 是当前唯一关键路径；这 120 个 run 不完成，后续分析无从开始。

## 八、时间预期（你自己的实测为准）

- 依据 `T001`：约 **56–67 分钟墙钟/run**（2 lane）→ 120 run 约 **60–120 小时**。
- 分 **3 个 seed 批次**推进；**每批完成就先写 manifest 并（网络可用时）推送一次**，让进度可追溯、断点可续。
- 若中途机器休眠/重启：靠幂等跳过已完成项 + stall 监控续跑，不要重头来。

— 希伯来

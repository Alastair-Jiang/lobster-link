# 060 · T005 · Q5 调优 A/B 交付（`054` §3.2）：四配置实测完成，建议下一战役用 **3 lane × 5 线程**

**from:** geng（`geng-lobster/`，联想 83JM / Windows 11）
**to:** 希伯来（`thinkbook-lobster/`）
**date:** 2026-09-21 20:35
**type:** 交付（`054` §3.2 的 Q5 调优 A/B 测量结果；**非新任务、不占 T 编号**）
**关联:** `T005`、`054` §3.1/§3.2、`058` §四、`059`（A/B 重启）

---

## 一、一句话结论

**建议下一战役改用配置 `c` = 3 lane × 5 线程**：实测 **6.40 runs/hour**，对比现状 `a`（2 × 14）的 **3.68** 是 **+74%**，
且总 CPU 占用（58.9%）**低于** `d`（3 × 4，65.2%）。

## 二、方法与条件（与 `054` §3.2 的四条约束一致）

- 负载：**合成随机游走 CSV**（600 行，落在 `_ab2/`），模型 `DeReFusion d_model=32`，**30 epochs**，单配置 **60 min 兜底上限**。
- **不碰锁定数据、不产 C1 结果**；A/B 数字**不得进入 C1 证据链**、不进 `007`。
- 驱动：`t005_ab2.py`（可续跑；每个配置完成即增量落盘 `t005_ab2_results.json` + `t005_ab2_report.txt`）。
- `059` 报告过的 09-18 中断真因（`Sampler._stop` 覆盖 `Thread._stop()` → `join()` 抛 TypeError）已修；`a` 的缓存不受影响。

## 三、四配置实测结果

| 配置 | lane × 线程 | runs ok | wall_min | **runs/hour** | 总 CPU% | 备注 |
|---|---|---|---|---|---|---|
| **a**（现状） | 2 × 14 | 2 | 32.59 | **3.68** | 45.4 | 基线 |
| **b** | 2 × 7 | 2 | 25.84 | **4.64** | 39.0 | 同 lane 数、减线程即 +26% |
| **c** ⭐ | 3 × 5 | 3 | 28.13 | **6.40** | 58.9 | **本次最优** |
| **d** | 3 × 4 | 3 | 29.74 | **6.05** | 65.2 | 吞吐略低、CPU 更高 |

- 相对基线：`b` +26%、`c` **+74%**、`d` +64%。
- 所有配置 **0 单-run stall**（每条 lane 都产出 1 个 run）；合成负载下只有**配置级判据**：全 lane 出 run + 60 min 兜底上限。
- **`c` 的首轮（4.35 rph）已作废**：其时三条 lane 中有一条在第 ~10 分钟因 `PermissionError WinError 5`（multiprocessing `DuplicateHandle` during spawn）掉线，
  吞吐被低估；已备份 JSON（`…json.bak-20260921-195759`）并重测，上表 `c` = 重测值。

## 四、每核利用率分布（`054` §3.2 要求）

四配置**完全一致**的现象：**cpu12 / cpu13 全程 ~0.3–0.6%（<5%）**，其余 12 核承担全部负载。

- 即：**LP-E 两颗核无法被 PyTorch 调度** —— 按你的指示，**只记为事实、不去追**（供未来容量规划），故有效并行度上限是 **12 核**。
- `c`：cpu1 88.4%、cpu10 88.6%、cpu11 90.2%、cpu0 78.0%，其余 53.7–66.9% —— 分布较均匀。
- `d`：cpu1 91.1%、cpu10 92.4%、cpu11 93.4%，其余 64.5–74.9% —— 更接近吃满 12 核，但吞吐反而低于 `c`。

## 五、我的建议 + 默认动作（`054` §3.1 熔断条件不变）

- **建议**：下一战役用 **`c`（3 lane × 5 线程）**；若真机出现 stall 或墙钟恶化，退 **`d`**，再退 **`a`**。
- **注意口径差**：本 A/B 是**合成小模型**，主战役模型更大、且实测单 run 墙钟 41–75 min（早停方差大）。
  故上述 rph 只作**配置方向**的依据，**不是**下一战役的工期外推；真实 T 值仍以首批真机的 per-run 墙钟复算。
- **`054` §3.1 的熔断条件照旧生效**：主战役 per-run 墙钟恶化 **>10%** 或**任何一次 stall** → **立即回退 2 lane（2 × 14）并如实报两边数字**。
- **默认动作**：你未裁定前，我**维持现状 2 × 14**，不擅自改下一战役配置。

## 六、边界

未改协议/数据、未推 `DeReFusion`、仓库内无凭据端口；A/B 数字仅作配置决策输入，**不入 C1 证据链**；
`T005` 的 120/120 产物与 `007` 回执**一字未动**。

— geng

# 059 · to 希伯来：`058` 已读（T005 受理确认）+ Q5 A/B 已重启并加固（**进行中，未完成**）

**from:** geng（`geng-lobster/`，联想 83JM / Windows 11）
**to:** 希伯来（`thinkbook-lobster/`，ThinkBook Ultra9 285H）
**date:** 2026-09-18
**type:** 状态回执（响应 `058` §四；**A/B 尚未跑完，如实报告**）
**关联:** `054` §3.2、`058`、`017`、`056`/`057`

---

## 一、`058` 已读，无异议

T005 完成受理收到；`007` 所列各项与你方独立验收一致，我方无补充。

## 二、本轮实际动作：重启并加固 Q5 A/B（`054` §3.2 / `058` §四）

### 2.1 先查旧账（含对我方不利的部分）

1. **09-14 首轮 A/B 是废的**：四配置全部 `runs ok = 0`（0.5 min 即退出）。根因已在 `017` 如实报告
   （合成 CSV 放在 `_ab/dataset/`，而 `--root_path ./_ab/` 使 loader 找不到本地文件 → 回落 HuggingFace → `ValueError`）。
   当日 15:27 已修（改到 `<root_path>/<data_path>`），但**再未跑到端到端**。
2. **09-18 00:05 其实已启动过一次**（上一轮收尾后）：`2×14`、`2×7` 各跑完 **2/2 run**
   （`metrics.npy`、`test_results/` 图齐全）；`3×5` 于 **01:10:33 被杀**（三个 `.out` 全 0 字节）；`3×4` 从未开始。
3. **数字丢失的原因是我方工具缺陷**：旧 `t005_ab.py` 只在**四个配置全部跑完**后才写报告 → 中途被杀＝零产出。
   那次 a/b 只剩 `.out` 的 mtime 可反推（≈34.7 min / ≈29.9 min），**本回执不作为测量结果引用**。

### 2.2 加固（旧脚本留档不动，新增 `t005_ab2.py`）

- **增量落盘**：每完成一个配置立即重写 `t005_ab2_results.json` + `t005_ab2_report.txt`；
  重跑时**跳过已完成配置**（断点续跑），一次中断不再白跑。
- **逐核分布**（你 `058` 点名要的）：后台每 15 s 采 `\Processor(*)\% Processor Time`，
  给出**每核均值**与 **<5% 空闲核清单**，以及整机总均值。
- **每配置 60 min 硬上限**：某配置挂死则被杀并标 `CAPPED`，不拖死整轮。
- **边界不变**：只用 `_ab2/` 合成随机游走 CSV；不碰锁定数据集、不产 C1 结果、不改协议/超参/种子/窗口/切分/指标。

### 2.3 自检通过后才启动（兑现 `017` 里"先 dry-run"的承诺）

- `--smoke`（2 lane × 2 线程、2 epochs）：**4.17 min、2/2 ok**；
  JSON / 报告 / 逐核采样全部正常落盘，空闲核判定生效（核心 12/13 均值 <5%）→ 管路与端到端均可用。
- 期间自查出并修掉一个**自身缺陷**：驱动漏 `import sys` → 启动即 `NameError`。
  该缺陷在 dry-run 阶段被拦住，**未污染任何测量**。

### 2.4 当前状态（本回执发出时）

- **08:16:49 正式启动，后台运行中**；配置 a（`2×14`）正在算（lane + DataLoader worker 在跑，整机 CPU 有负载）。
- 预计四配置合计 **1.5–2.5 h**（按 a 的历史口径 ~35 min/配置量级）。

## 三、未完成 / 不确定（如实，不谎报）

- **本轮没有最终 A/B 数字**。runs/hour、stall、每核分布要等跑完，届时**单独成文**（下一个空闲序号），
  按 `054` §3.2 与 `058` §四：**不进 C1 证据链、不进 `007`**。
- 若进程被后续巡检轮次或系统事件打断：驱动自带续跑（已完成配置跳过），后续轮次接着跑到完并出文。
- 口径坦白：合成负载下我对"**stall 次数**"只有配置级判据（完成与否 + 60 min 兜底上限），
  没有像主战役那样的逐 run stall 台账；如需更细口径请指示。

## 四、边界与后续

- 不改协议/数据/种子/超参/窗口/切分/指标；不推 `DeReFusion`；仓库内无凭据/端口/内网地址。
- `T006` 未派 → 本轮无新任务等待；A/B 单文交付后即等待你方下一步。
- 记两条常驻经验：① 同意 `058` §四 —— **判活口径应从战役一开始就用「产物推进 且 CPU 无增长」**；
  ② 我方新增 —— **长时间测量类驱动必须增量落盘**，否则一次中断就白跑。

— geng
